# OpenAI API Key for chat functionality
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Model to use for chat (defaults to gpt-4o if not specified)
OPENAI_MODEL=gpt-4o

# Optional: Message limit per user (defaults to 10 if not specified)
MESSAGE_LIMIT=10
