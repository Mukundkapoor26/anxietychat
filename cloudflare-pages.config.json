{
  "build": {
    "command": "npm install --no-package-lock --legacy-peer-deps && npm run build",
    "environment": {
      "NODE_VERSION": "18",
      "NPM_FLAGS": "--legacy-peer-deps",
      "NEXT_TELEMETRY_DISABLED": "1"
    },
    "output_directory": ".next/standalone"
  }
}
